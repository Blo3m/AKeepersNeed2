# Build output
bin/
obj/

# Visual Studio / Rider IDE state
.vs/
.idea/
*.user

# Temp / backup files
*.tmp

# Markdown docs are kept local-only (only the root README.md stays tracked)
*.md
!/README.md

# Game assemblies + decompiles are local, unversioned references (large binaries).
# Copy them from the game folder; see CLAUDE.md.
dllrefs/

# Machine-local references to the game's copyrighted DLLs (generated by build.ps1).
GameReferences.props
